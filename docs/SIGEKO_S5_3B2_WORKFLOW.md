# S5.3b2 – verbindlicher Workflow-/Dateivertrag

Basis main c7103ae5e74795defb77fdc9d15809054d9cb983; Branch codex/sigeko-s53b2-vorankuendigung-pdf. Führend #251/#274/#277 mit bestätigtem aktuellem Kommentarstand. S5.3b1 ist PR #337 integriert, Baseline 1858 PASS / 97 unveränderte Fehler. Goal-Lauf mit getrennten Analyse-/Umsetzungs-/Reviewaufträgen; Hauptagent koordiniert gemeinsame Schnittstellen. Kein Folgepaket S5.4–S9 vorgezogen.

## Vollständige Ausgabeentscheidung vor Code

A. UI und PDF. Das vorhandene Vorankündigungsformular erhält Vorschau, finale PDF-Erstellung, Layoutstart und Öffnung eigener vorhandener Fassungen. Unter dem vorhandenen BBM-V2-Kopf stehen formularnah die Punkte 1–9, zwei Koordinatorbereiche und der leere Unterschriftsbereich.
B. Präsentation editorfähig über vorhandenes M83/Profil bzw. deklarativen PDF-Adapter. Kein neuer Editor-/PDF-Core.
C. Vollständige konkreten Elemente, IDs, sechs DOM-Attribute, Parents, Baselines, Grenzen, erlaubte/gesperrte Operationen: SIGEKO_S5_3B2_PDF_ENTWURF.md (81 PDF-Ziele) und SIGEKO_S5_3B2_UI_ENTWURF.md (13 neue UI-Ziele, 111 Pflichtslots/112 Scopeziele). Diese Tabellen sind Teil dieser Entscheidung. Kein fehlendes Element wird später aus DOM/Fachwerten erraten.
D. Erstellen, Vorschau, Dateizugriff, Auswahl, Datenhaltung, IPC und Layoutstart bleiben Fachaktionen. Editor verändert nur Präsentation. Keine Tabellen-/Spaltenfreigabe: native Fassungswahl ist eine Bedienauswahl; Firmenliste bleibt unverändert bestehende Ausgabe.
E. Jeder neue Parent ist deklariert; alte UI-ID-/Orderwerte bleiben erhalten. PrintShell bleibt Eigentümer von Seite, V2-Kopf, Fußreserve und Einseitenvertrag. Keine neue Paginierung oder Umbruchoperation.
F. Kit-/M83-/gemountete Refprüfungen, tatsächliche PDF-Targets, gemessener Textüberlauf mit Abbruch, Profil/Undo/Restore/Regeneration, reale Electron-PDF-/Formularabläufe unter Windows/Linux. PDF-V2-SATZ-001/002/003/005/013 und alle 49 vorhandenen Golden-Seitenzahlen/Struktursnapshots unverändert prüfen. Eigene PDF-V2-SIGEKO-VA-001–008 nach PDF-Entwurf. Volltest mit exaktem Fehlervergleich. Diese neuen Nachweise sind noch auszuführen, nicht bereits bestanden.

## Erlaubte Produktbereiche und gemeinsame Anschlüsse

Gemeinsame Anschlüsse in printIpc.js, pdfProviderBridge.js, PrintShell.js und kleinem Provider-Dispatch in ProviderDocument.js; keine neue Firmenrenderer-/Pager-/Invoiceimplementierung. Technischen gemeinsamen Anschluss separat committen und über bestehende Printjob-/Providerprüfungen testen. Fachprovider/Renderer/Descriptor unter vorhandenen SiGeKo-/Print-/UI-Editor-Verzeichnissen; vorhandene Modulkomposition lädt sie. Datenhaltung additiv in sigekoSchema.js und neuem sigekoDocumentsRepo.js, Main-Fachworkflow in PreNotificationDocumentService.js, modulare IPC/preload-Ergänzungen. Bestehende Projekttransfergrenze um finales V8-Dokumentpayload erweitern. Vorhandenes Formular/komponentennaher Vertrag, Registryversion/Manifest und entsprechende Tests/CI. Keine Rechnung, kein Router-/MainHeader-/Kit-Umbau.

Der bestehende getPrintRuntimeContext bleibt gemeinsame Quelle. Neuer VA-Provider liefert einen geprüften Main-Snapshot; Bridge gibt bereits validierte äußere identity {moduleId,projectId,documentId} an provide weiter und übernimmt optionalen ausschließlich vom registrierten Provider gelieferten printRuntimeContext. Dessen Projektidentität und Hochformat prüfen; mode/documentTypeId/projectId/documentId/orientation/tableLayouts abschließend aus validierter Anfrage festsetzen. Der VA-Provider nimmt ausschließlich Main-Kontext-ID oder gespeicherte Dokument-ID an und prüft Projekt/Dokument; keine maßgeblichen Clientschattenkopien. Technischer Provider bleibt unverändert.

PrintShell erhält für deklarierte einseitige Provider einen ausdrücklichen Standard-FullHeader-Modus; bestehender Sondertitel-Inhaltsslot bleibt gleich. VA setzt im eingefrorenen gemeinsamen Kontext printProfile.documentLabel='Vorankündigung' und header.titleMode='documentLabel'. Keine kopierte Kopfkomponente. Papier/Schriften/Positionierung folgen dem gesonderten vollständigen PDF-Entwurf. Zu langer Inhalt wird konkret benannt abgewiesen, nicht gekürzt oder unbemerkt verkleinert.

## Vorbereitete Druckdaten und Dateigrenze

Bestehender PDF-Job lädt momentan zweimal. Für eine Firmenanlage dürfen zuvor geprüfte Daten nicht durch spätere Live-Firmen ersetzt werden. Enger Main-interner optionaler zweiter Parameter für printToPdf: vorbereitete geklonte Daten, synchroner beforeWrite-Guard, exclusiveWrite und includeMetadata. Kein solcher Eingang über IPC/preload. Datenmodus/Projekt/Hochformat müssen zur validierten Anfrage passen. Vorbereitete Daten werden an tatsächliche webContents und genaue jobId gebunden; fehlende/falsche ID fällt niemals auf Live-Daten zurück. Rückgaben klonen, weil vorhandenes getData Editor-/Versionsmetadaten ergänzt. Auch print:ready benötigt bei vorbereitetem Job genaue ID. Erfolg/Fehler/Timeout/Fenster-/Rendererende räumen gebundene Daten auf.

Vorbereitung ersetzt keine bestehende Lizenz-/Projekt-/Storageprüfung. beforeWrite unmittelbar vor Dateiablage nach printToPDF synchron ausführen; Promise/Thenable verweigern. Exklusives wx-Schreiben, tatsächlicher Bufferhash SHA-256 und byteSize in optionalen Metadaten. Legacy ohne neue Main-Optionen bleibt unverändert. Die bestehende gemeinsame Firmenausgabe ist bereits exklusiv. Kein eigener Printprozess, keine frei steuerbare neue Ausgabepfad-API.

## Fachlicher Ablauf

Main-Endpunkte: previewPreNotificationPdf, createPreNotificationPdf, listPreNotificationDocuments, openPreNotificationDocumentFile und preparePreNotificationPdfEditor; durch vorhandenen SiGeKo-IPC-Registrar mit laufender Modullizenz geschützt. Renderer übergibt nur Projekt-ID/gelesene Revision bzw. gespeicherte Dokument-ID und Dateikind. Keine freie Datei, keine eingefügten Snapshotwerte. Standard-Projektablage über vorhandenen createProjectStorageAccess, Ziel Unterlagen; kein neuer Pfadstore.

Vorschau und Editor: gespeicherten Entwurf über S5.3b1 Capture erfassen; keine Entwurfs- oder Dokumentzeile allein durch Vorbereitung. Ein Main-eigener begrenzter Kontext dient dem vorhandenen Provider-/Regenerationsweg. Vorschaujob-Kontext nach Ende freigeben. Editor-Kontexte maximal vier, zeitlich begrenzt auf 24 Stunden und bei Appende verworfen; alte/abgelaufene IDs liefern klaren Fehler statt Live-Neuauflösung. Neues vorbereiten ersetzt keine historische Datei. Layoutstart braucht keine zuvor erfolgreiche PDF und verwendet vorhandenen nativen PDF-Ausgabe-Reiter, keine neue Kit-API.

Finale Erstellung: aktives/schreibbares Projekt, aktuelle Lizenz und aktuell bestätigte zuständige Arbeitsschutzbehörde prüfen. Readiness bleibt Warnung und wird nicht pauschal zur zusätzlichen Berechtigung. Main erfasst unveränderlichen S5.3b1-Snapshot. Bei Firmenmodus attachment tatsächliche gemeinsame Firmen-Druckdaten einmal Main-seitig auflösen, aktive nicht leere Firmen prüfen und unverändert über bestehenden Renderer als Anlage erzeugen. Danach VA aus eingefrorenem Main-Kontext drucken. Während Ausgabe und unmittelbar vor finaler Einfügung Lizenz, Projekt-/Storageidentität und aktuelle Behördenvoraussetzung erneut prüfen. Erst tatsächliche Haupt-PDF plus gegebenenfalls Anlage validieren und finalen DB-Datensatz einmal einfügen. Es gibt keine vorläufige DB-Fassung und keine neue Ampelstufe.

Fehler erhalten alle bestehenden Fassungen. Bereinigt werden höchstens nachweislich durch diesen Auftrag erzeugte und bytegleich gebliebene Dateien. Fremde/veränderte Dateien nicht überschreiben/löschen. Absturz kann unreferenzierte Dateien hinterlassen; keine erfolgreiche Fassung vor fertigen Dateien behaupten. Kein allgemeiner DB-/Dateisystem-Transaktionscore in diesem Fachpaket.

## Finale Datenhaltung

Neue additive Tabelle sigeko_documents mit id (globale stabile ID), project_id (FK projects, ON DELETE CASCADE), document_type (hier sigeko-vorankuendigung), snapshot_json, files_json und created_at. Index für Projekt/Typ/Erstellzeit/ID. JSON-Identität muss mit Zeilen-ID/Projekt/Typ übereinstimmen; S5.3b1-Vertrag validiert Snapshot. Bestehende Fassungen nicht aktualisierbar, keine fachliche Delete-/Update-API. Keine neue nummerierte allgemeine Dokumentenplattform.

files_json enthält geordnet genau eine Haupt-PDF (kind:'main') und bei snapshot.form.firmsMode==='attachment' genau eine Firmenanlage (kind:'firms'). Je Datei ausschließlich kind, projectRelativePath, sha256, byteSize. Relative Referenz unter SiGeKo/Unterlagen, keine absoluten Pfade oder Traversierung. SHA-256 aus tatsächlichem ausgegebenem Buffer muss mit gespeicherter Datei übereinstimmen; normale PDF-Datei und positive Größe prüfen. Snapshot-Erfassungszeit bleibt im Snapshot, erfolgreicher Ablagezeitpunkt in created_at. Keine späteren Live-Firmendaten als historische Anlage verwenden.

Dokumentbestand liefert nur projektzugehörige Metadaten, deterministisch neueste zuerst. Öffnen prüft ID/Typ/Projekt, gespeicherte Datei/Hash und kontrollierten Projektpfad und verwendet vorhandene interne PDF-Vorschau. Auswahl früherer Fassungen verändert den Entwurf nicht. Veränderte/fehlende Dateien sichtbar melden. Projektordner wird wie bisher aus veränderbaren Projektbezeichnungen abgeleitet: ohne vorhandene Umbenennungsmigration wird nicht heimlich ein alter Pfad geraten oder eine historische Datei neu erzeugt.

## Projekttransfer V8

V8 nur bei finalen sigeko_documents. Obligatorisches vollständiges data/sigeko_documents.json und exakte Counts; alte V1–V7 unverändert. Heruntergestufte Archive mit Dokumentpayload verweigern. Zeilen-/Snapshot-/Dateiidentität, Duplikate, Typen, Dateireferenzen und Hash/Größe vor Schreiben prüfen. Vor destruktivem Export alle referenzierten PDF-Dateien am aktuellen gemeinsamen Projektpfad prüfen; fehlende historische Dateien verhindern Entfernen des Quellprojekts. Dateien reisen im vorhandenen project-folder mit, keine separate Dateiplattform.

V8-Import reserviert den neuen Zielordner exklusiv mit mkdir ohne recursive am Ziel selbst; vorhandene Ordner werden verweigert. Erst Dateien in diesen eigenen Ordner kopieren und sämtliche Dokumentdateien dort nach Hash und Größe prüfen, danach die bestehende synchrone DB-Transaktion einschließlich Dokumentzeilen ausführen. Bei Kopier-, Prüf- oder Transaktionsfehlern nur den von diesem Import exklusiv reservierten Ordner bereinigen. Kein Dokumentdatensatz vor fertigen Dateien. Absturz kann einen unreferenzierten Ordner hinterlassen. Der bestehende V1–V7-Ablauf bleibt unverändert.

V8-Export prüft zusätzlich die tatsächlich im fertigen ZIP enthaltenen Dokumentdateien durch temporäres Entpacken und Hash-/Größenvergleich, bevor das Quellprojekt entfernt wird. Unmittelbar vor der Löschung aktuelle Projektzeile und vollständige Projektdaten gegen den exportierten Stand vergleichen und Quelldokumentdateien erneut prüfen; Datenvergleich und bestehendes deleteForever in einer gemeinsamen synchronen DB-Transaktion ausführen. Abweichungen verhindern die Löschung und melden erneuten Exportbedarf. Keine allgemeine Atomaritätsgarantie für externe Dateisystemänderungen behaupten. Reale V8-ZIP-Roundtrips, Kopier-/Transaktionsfehler, veränderte Archive und alte V1–V7 prüfen.

## Abnahmekriterien

Keine erfolgreiche finale Dokumentzeile bei Render-, Lizenz-, Pfad-, Schreib- oder Anlagenfehlern; zwei erzeugte Fassungen und alte Datei unverändert; Byte-/Hashprüfung, Projektisolation und Archivschutz. Reale Firmenanlage verwendet einmal erfasste bestehende Druckdaten. Vorschau/Editor schreiben nur temporär. 81 PDF-Ziele vollständig, TextResize nur erlaubte Ziele, leere Signatur, getrennte Rollen/Zahlen, Umlaute und lange Inhalte ohne stilles Abschneiden. 13 UI-Ziele und alle 111 Pflichtrefs, 112 Scopeziele, Registry36, alte Scopes unverändert; Aktionen auch bei 560×480 und tiefem Scroll erreichbar. Windows-/Linux-Electron/PDF/GUI und exakte Baseline, unabhängiger Review, PR/Integration und Dokumentation; anschließend direkt S5.4.
